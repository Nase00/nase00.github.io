import { DARK_GREY, WHITE } from 'styles/colors';

const styles = {
  '.body-container': {
    overflow: 'hidden',
    position: 'absolute',
    height: '100%',
    width: '100%',
    textAlign: 'center',
    backgroundColor: DARK_GREY
  },
  'input, label': {
    color: WHITE
  }
};

export default styles;
